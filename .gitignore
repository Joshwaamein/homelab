# Sensitive inventory files with real IPs and hostnames
ansible/inventory
ansible/noble-semaphore/inventory
ansible/noble-semaphore/users.txt
ansible/users.txt

# UFW firewall configuration (reveals network architecture and ports)
ansible/noble-semaphore/config-ufw.yml

# Ansible Vault files (encrypted credentials)
**/vault.yml

# Exclude entire semaphore directory (contains personal configs, VS Code server files, ansible roles)
semaphore/

# VS Code directories
.vscode/
.vscode-server/

# Logs
*.log

# SSH and temporary files
*.sock
*.swp
*~
